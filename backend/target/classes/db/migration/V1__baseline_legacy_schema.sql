-- V1 : baseline Flyway pour schémas créés historiquement via Hibernate (ddl-auto).
-- Les tables métier (users, products, sales, …) doivent déjà exister avant V3+.
-- Migration no-op volontaire.
SELECT 1;
